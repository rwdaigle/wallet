<tr>
  <td><%= e.recipient %></td>
  <td>$<%= number_with_precision(e.credit, precision: 2) %></td>
  <td>$<%= number_with_precision(e.debit, precision: 2) %></td>
  <td>$<%= number_with_precision(e.balance, precision: 2) %></td>
  <td><%= link_to 'Show', e %></td>
  <td><%= link_to 'Edit', edit_exchange_path(e) %></td>
  <td><%= link_to 'Destroy', e, method: :delete, data: { confirm: 'Are you sure?' } %></td>
</tr>
